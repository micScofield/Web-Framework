{
  "devDependencies": {
    "concurrently": "^6.3.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "axios": "^0.24.0"
  },
  "scripts": {
    "db": "json-server -w db.json",
    "parcel": "parcel index.html",
    "start": "concurrently \"npm run db \" \"npm run parcel\""
  }
}
